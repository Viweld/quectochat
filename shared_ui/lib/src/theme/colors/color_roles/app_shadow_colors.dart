import 'package:flutter/material.dart';

/// Cast / elevation shadow ink — always deeper than surfaces (not inverted in dark).
final class AppShadowColors {
  const AppShadowColors({required this.main});

  final Color main;
}
