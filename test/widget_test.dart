import 'package:flutter_test/flutter_test.dart';
import 'package:quectochat/main.dart';

void main() {
  test('Application widget can be constructed', () {
    expect(const Application(), isA<Application>());
  });
}
