name: viewers
publish_to: none
description: Media viewers — fullscreen image (and future video/etc.) screens.

environment:
  sdk: '>=3.8.0 <4.0.0'
  flutter: '>=3.38.0 <4.0.0'

resolution: workspace

dependencies:
  flutter:
    sdk: flutter

  shared_core:
    path: ../../shared_core
  shared_ui:
    path: ../../shared_ui
  navigation_api:
    path: ../../navigation_api

  auto_route: ^11.1.0
  injectable: ^3.0.0
  get_it: ^9.2.1
  photo_view: ^0.15.0
  cached_network_image: ^3.4.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.15.1
  injectable_generator: ^3.0.2
  auto_route_generator: ^10.5.0
